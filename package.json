{
  "name": "@dropb/ffprobe",
  "version": "1.0.1",
  "description": "ffprobe wrapper",
  "main": "lib/index.js",
  "typings": "lib/index.d.ts",
  "scripts": {
    "build": "npm run tslint && npm run build-ts",
    "watch": "tsc-watch",
    "release": "standard-version",
    "test": "./node_modules/.bin/mocha --require ts-node/register ./test/*.spec.ts --timeout 5000",
    "test:watch": "./node_modules/.bin/mocha --watch --watch-extensions ts --require ts-node/register ./test/*.spec.ts --timeout 5000",
    "build-ts": "tsc",
    "tslint": "tslint -c tslint.json -p tsconfig.json"
  },
  "author": "kukhariev",
  "repository": {
    "type": "git",
    "url": "https://github.com/kukhariev/ffprobe.git"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/chai": "^4.1.2",
    "@types/mocha": "^2.2.48",
    "@types/node": "^9.4.5",
    "chai": "^4.1.2",
    "mocha": "^5.0.0",
    "standard-version": "^4.3.0",
    "ts-node": "^4.1.0",
    "tsc-watch": "^1.0.16",
    "tslint": "^5.9.1",
    "typescript": "^2.7.1"
  },
  "dependencies": {},
  "publishConfig": {
    "access": "public"
  }
}
